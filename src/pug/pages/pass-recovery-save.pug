extends ../layout/empty-layout

block variables
  -
    const lang = "ru";
    const title = "ICRM";
    const bodyClassName = "empty-layout";

block content
  main.login-page.login-page--pass-recovery
    form( action="/" method="POST" )
      h1.title.title-middle Новый пароль
      .form-group
        .input-field
          input( id="register-pass" type="password" class="validate" )
          i.material-icons.tiny vpn_key
          label( for="register-pass" ) Новый пароль
          small Введите пароль
        .input-field
          input( id="register-pass" type="password" class="validate" )
          i.material-icons.tiny vpn_key
          label( for="register-pass" ) Повтрите пароль
          small Введите пароль

        button.btn.btn-green.waves-effect.waves-light( type="submit" name="action" ) Сохранить
